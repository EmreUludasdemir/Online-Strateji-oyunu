Original prompt: Build a browser-based online strategy game MVP with a React + Vite + TypeScript frontend, Phaser map screen, Node + TypeScript authoritative backend, PostgreSQL + Prisma persistence, Docker Compose local setup, pnpm workspace, authentication, resources, buildings, world map, combat, seed data, tests, and a complete README.

- 2026-03-07: Initialized greenfield monorepo layout plan for `apps/web`, `apps/server`, and `packages/shared`.
- 2026-03-07: Confirmed workspace was empty, enabled `pnpm` through Corepack, and selected raw WebSocket notifications plus CSS Modules.
- 2026-03-07: Added the pnpm workspace, shared DTO package, Express + Prisma backend, React + Phaser frontend, Docker Compose Postgres, Prisma migration, demo seed, README, and test harness.
- 2026-03-07: Validated `corepack pnpm test`, `corepack pnpm build`, database migration + seed, and browser flows for login, dashboard, world map, attack, and reports.
- 2026-03-08: Split the Phaser map into lazy-loaded frontend chunks, added a selectable settlement list, and improved auth form autofill behavior.
- 2026-03-08: Added map intelligence cards plus per-settlement attack/defense previews so raid decisions are clearer before committing.
- 2026-03-08: Reworked the game UI toward a brass-and-parchment strategy look with stronger typography, richer resource plaques, district stamps, and a more structured city overview panel.
- 2026-03-08: Kept the new map frame styling, but Playwright full-page capture still shows a black Phaser canvas in headless mode even after frontend restarts; dashboard previews remain reliable.
- 2026-03-08: Added a governor-style command layer: dashboard focus cards, build recommendations, war council guidance, march-planning metrics on the map, and battle-ledger summary cards.
- 2026-03-08: Docker Desktop had been closed during validation, which caused temporary `500` responses from `/api/auth/me` and `/api/game/state`; restarting Docker and Postgres restored normal operation.
- 2026-03-08: Refactored the backend into `commands`, `queries`, `reconcile`, and `events`, then expanded the Prisma schema for troops, commanders, research, marches, fog tiles, and richer battle reports.
- 2026-03-08: Replaced instant attack resolution with march creation, ETA-based resolution, compatibility attack wrapping, chunked `64x64` map queries, and WebSocket events for training/research/march/fog/report updates.
- 2026-03-08: Added Kingdom Core frontend panels for army training, commander visibility, doctrine research, march dispatch, march recall, fog-of-war rendering, and richer `render_game_to_text()` output.
- 2026-03-08: Added self-healing city infrastructure backfill so legacy demo users automatically gain the new barracks, academy, watchtower, troop, research, and commander data on first authenticated access.
- 2026-03-08: Moved session TTL and auth/command rate-limit knobs into environment variables and updated `.env.example`.
- 2026-03-08: Added `scripts/smoke_kingdom_core.mjs` plus the root `smoke:e2e` script for repeatable Playwright verification against live dev servers.
- 2026-03-08: Re-ran `corepack pnpm build`, `corepack pnpm test`, the copied `web_game_playwright_client.js` smoke loop, manual Playwright MCP browser validation, and `corepack pnpm smoke:e2e`; all passed.
- 2026-03-09: Added Social World v1-lite alliance support with Prisma models for alliances, members, chat, help requests, and help responses, plus migration `20260309122022_alliance_social_v1`.
- 2026-03-09: Implemented alliance backend flows for create, join, leave, chat, request help, and respond help, and wired `alliance.updated` notifications into the existing WebSocket invalidation loop.
- 2026-03-09: Added the `/app/alliance` frontend route with roster, chat, queue-help controls, and seeded the demo alliance `Bronze Concord [BRZ]`.
- 2026-03-09: Extended server smoke coverage with an alliance test and updated `smoke:e2e` to verify the seeded alliance chamber before the march/report flow.
- 2026-03-09: Re-ran `corepack pnpm db:migrate --name alliance_social_v1`, `corepack pnpm db:generate`, `corepack pnpm db:seed`, `corepack pnpm build`, `corepack pnpm test`, `corepack pnpm smoke:e2e`, and the copied `web_game_playwright_client.js` smoke loop; all passed.
- 2026-03-11: Started Phase 2A implementation. Extended shared DTOs and Prisma schema for `MapPoi`, POI enums, march objectives/cargo, and unified report entries, and added migration `20260311101500_phase2a_poi_social`.
- 2026-03-11: Reworked backend march orchestration for `CITY_ATTACK`, `BARBARIAN_ATTACK`, and `RESOURCE_GATHER`, added deterministic POI seeding/backfill, response-time alliance fog union in world chunk queries, unified report reads, and a multi-stage reconcile loop with POI respawns.
- 2026-03-11: Finished the Phase 2A frontend pass: map selection now supports cities and POIs, Phaser renders camps/nodes with objective-colored march paths, reports render unified city/PvE/gather entries, and `render_game_to_text()` exposes POI state for browser automation.
- 2026-03-11: Added server API coverage for barbarian camp PvE, resource gathering/occupancy conflict, and alliance chunk vision share; updated `scripts/smoke_kingdom_core.mjs` to prefer POI flows when a visible node or camp exists.
- 2026-03-11: `corepack pnpm build` passes. `corepack pnpm --filter @frontier/server test` is currently blocked because the Postgres test database at `localhost:5433` is unavailable, and `corepack pnpm smoke:e2e` is blocked because the web and websocket dev servers are not running on `localhost:5173` and `localhost:3101`.
- 2026-03-11: Started Phase 2B with city battle windows. Added `BattleWindow` persistence, `MarchState.STAGING`, city-attack ETA staging into a `15s` window, and delayed sequential resolution for all staged marches when the window closes.
- 2026-03-11: Updated active march views and map UI so staged city attacks show a countdown to battle-window close, and adjusted server tests to expect stage-then-resolve instead of immediate city battle resolution.
- 2026-03-11: Added Phase 3 foundations: in-memory ops metrics at `GET /api/ops/metrics`, request/rate-limit/reconcile/realtime instrumentation, a Redis-ready realtime adapter boundary that still falls back to in-memory delivery, authenticated product analytics ingest at `POST /api/game/analytics`, frontend analytics hooks for tutorial/upgrade/train/march/battle/research milestones, and a noop store-validation port for later IAP wiring.
- 2026-03-11: Phase 3 is still partial: metrics are JSON snapshots rather than OpenTelemetry exporters, Redis fanout is not implemented behind the adapter yet, analytics only log to server stdout, and store receipt validation remains a placeholder.
- 2026-03-11: Re-skinned the frontend into a warmer imperial style with Ottoman-inspired parchment, brass, copper, and Iznik-blue accents across auth, dashboard, map, reports, and the Phaser world-map frame. Navigation labels and page copy now lean into the divan/atlas/ledger language while keeping the product original.
- Note: The frontend now lazy-loads the Phaser map route, but the dedicated Phaser vendor chunk is still large enough to trigger a Vite warning.
- Note: Browser validation uses the copied `scripts/web_game_playwright_client.js` because the original skill script lives outside the writable workspace and could not resolve local dependencies from there.
- TODO: Consider swapping Phaser for a lighter renderer or trimming Phaser imports if bundle size becomes a release concern.
- TODO: Extend Phase 2B battle windows to barbarian camps and then expose defender-side/alliance-side battle-window context in the map UI if coordinated siege play becomes a priority.
- TODO: Add dedicated frontend component tests and a fuller automated Playwright suite if this MVP grows beyond the current smoke-level browser validation.
- 2026-03-13: Completed the mobile-first HUD second pass for legacy screens. Rebuilt `AuthPage` as a TR-first onboarding surface with demo-account guidance, rewrote `ReportsPage` into a dedicated battle-log layout with inbox linkage, and moved `AlliancePage` onto the new primitive card system with announcement, marker, log, and contribution panels.
- 2026-03-13: Fixed the remaining dashboard text corruption (`Yukseltme hatti`) and verified the refreshed `login`, `alliance`, and `reports` routes in a live browser session after the existing dashboard/map HUD refresh.
- 2026-03-13: Re-ran `corepack pnpm --filter @frontier/web build`, `corepack pnpm --filter @frontier/web test`, and `corepack pnpm smoke:e2e`; the smoke flow passed with `Ashwatch -> Barbarian Camp 5` on the refreshed UI.
- Note: The browser console still shows an initial `404 /api/auth/me` during the login route in dev mode; this did not block login, route transitions, or the smoke flow, but it should be traced separately if auth proxy behavior is tightened.
- TODO: Finish the design-system split by extracting a dedicated tokens file and moving more English gameplay labels from server/shared constants into UI-facing TR/i18n presentation layers.

- 2026-03-13: Completed a full English normalization pass across the active web runtime. Converted GameLayout HUD copy, the alliance surface, inbox/target sheets, auth/dashboard/map/reports shell text, and smoke selectors to clear English while keeping gameplay contracts unchanged.
- 2026-03-13: Re-ran corepack pnpm --filter @frontier/web build, corepack pnpm --filter @frontier/web test, and corepack pnpm smoke:e2e; all passed with the updated English labels and selectors.
- Note: The latest smoke screenshot shows the refreshed HUD in English; the Reports route still transitions through a visible loading state during capture, but the smoke flow resolved successfully and rendered an English report heading.

- 2026-03-13: Confirmed the live local game is reachable at http://localhost:5173/login. Verified demo login with demo_alpha in a browser session and loaded the refreshed English dashboard/HUD successfully.

- 2026-03-13: Began world-space Frontier Map refactor. Added shared camera/detail helpers, rewired MapPage chunk fetching to camera center, and replaced WorldMap with a persistent layered Phaser scene using world coordinates, pan/zoom, selection rings, and animated march groups.
- 2026-03-13: Fixed the first runtime regression in the new map scene by guarding camera-dependent resize/emit/focus paths until Phaser scene activation; map route validation then passed in browser and smoke runs.
- 2026-03-13: Validated the world-space map in a live browser session. The map route now renders without runtime errors, `render_game_to_text()` exposes camera center/zoom/detail/radius, `window.select_map_poi` still works, and the Zoom In control updates camera state from `1.00/mid` to `1.18/near`.
- 2026-03-13: Re-ran `corepack pnpm --filter @frontier/web build`, `corepack pnpm --filter @frontier/web test`, `corepack pnpm smoke:e2e`, and `node scripts/web_game_playwright_client.js ...`. The map refactor passed build/tests and an initial smoke run; a later smoke retry failed because the demo account had no troops available for `March to Camp`, not because of a map runtime regression.
- 2026-03-13: Extended the map pass with client-side scout trails, merged camera-neighbor chunk caching, and selectable active marches. `MapPage` now prefetches nearby chunks and falls back to scout missions when the local demo account has no troops, while `WorldMap` renders transient scout route FX and supports march-focused selection state.
- 2026-03-13: Hardened the browser smoke flow so it can validate either a real march or a scout-report path. The smoke script now opens the inbox and asserts `Scout report: <target>` when troopless demo data makes march buttons unavailable.
- 2026-03-13: Fixed a world-chunk server regression introduced by aggressive camera-prefetching. `getWorldChunk()` now runs with a longer Prisma interactive transaction timeout (`maxWait 10s`, `timeout 15s`), which removed a reproducible `500 /api/game/world/chunk` during live map browsing and target selection.
- 2026-03-13: Re-ran `corepack pnpm --filter @frontier/web build`, `corepack pnpm --filter @frontier/web test`, `corepack pnpm --filter @frontier/server build`, `corepack pnpm smoke:e2e`, the local `scripts/web_game_playwright_client.js` capture, and a live Playwright browser pass over `/app/map`. Validation confirmed zero browser console errors after the timeout fix, a working zoom transition into `near` detail, and a successful scout mission producing `Scout report: Barbarian Camp 5`.
- TODO: Add a deterministic dev-only march fixture or a dedicated smoke user with nonzero troop stock so on-map march selection and the `March Orders` sheet can be validated every run without manual troop training.
- 2026-03-13: Added a dedicated animation pass across the active frontend. Global motion now includes ambient background drift, richer entry keyframes, sheen sweeps, and a reduced-motion fallback; UI primitives gained stronger hover/press feedback and staggered card/resource reveals across the shell, dashboard, and map surfaces.
- 2026-03-13: Increased world-map motion without adding new art assets. Cities and POIs now breathe with ambient aura pulses, labels float subtly, moving marches emit trailing dust, and scout trails leave lighter transient particles while preserving the existing world-space Phaser architecture.
- 2026-03-13: Re-ran `corepack pnpm --filter @frontier/web build`, `corepack pnpm --filter @frontier/web test`, `node scripts/web_game_playwright_client.js --url http://localhost:5173/login ...`, and `corepack pnpm smoke:e2e` after the animation pass. Build/tests remained green and the smoke flow still completed via `Scout report: Barbarian Camp 5`.
- 2026-03-18: Continued the map polish pass with camera inertia and focus feedback. Dragging the world map now eases out instead of stopping abruptly, zoom clamping uses a shared camera-bound helper, and camera focus actions emit a local pulse so rail/march recenter actions feel deliberate.
- 2026-03-18: Added a `Recenter` control to the map HUD and updated march selection so choosing an active march also focuses the map camera on that unit. This keeps the new world-space scene navigable after long pans and makes the `March Orders` sheet easier to use.
- 2026-03-18: Re-ran `corepack pnpm --filter @frontier/web build`, `corepack pnpm --filter @frontier/web test`, the local `scripts/web_game_playwright_client.js` capture, and `corepack pnpm smoke:e2e` after restarting the local Postgres container. Validation stayed green; the smoke flow completed through `Scout report: Barbarian Camp 3`.
- 2026-03-18: Added a live minimap and macro region overlay to the world map. `MapPage` now renders a clickable SVG minimap with visible/discovered fog state, city/POI markers, viewport framing, and named frontier regions, while `WorldMap` renders matching macro region labels and divider lines at mid/far zoom.
- 2026-03-18: Wired minimap clicks into camera focus commands and validated the full loop in-browser: clicking the minimap moved the camera from `32,32` to `53,11`, and the active chunk re-centered there after the throttled refetch settled.
- 2026-03-18: Re-ran `corepack pnpm --filter @frontier/web build`, `corepack pnpm --filter @frontier/web test`, and `corepack pnpm smoke:e2e` after the minimap pass. All remained green, and the smoke flow still completed via `Scout report: Barbarian Camp 5`.
- 2026-03-18: Extended the world map with alliance marker overlays, allied-city tinting, and stronger march-resolution FX. `MapPage` now loads alliance state on the map route, paints alliance markers onto the minimap, exposes quick-focus marker buttons, and passes allied usernames/tag/markers into `WorldMap` for city coloration and map-pin rendering.
- 2026-03-18: Added impact polish to Phaser march teardown. Marches that resolve at their target now emit a layered pulse + shockwave + spark burst, while returning gather marches use a lighter cargo-return burst. This keeps recall noise lower and gives actual battle closures more weight.
- 2026-03-18: Validated the alliance overlay loop in a live browser session by creating the `South Pass` alliance marker on `/app/alliance`, then confirming the `/app/map` minimap and marker quick-focus button re-centered the camera and active chunk from `32,32` to `30,30`. Browser console errors remained at `0`.
- 2026-03-18: Re-ran `corepack pnpm --filter @frontier/web build`, `corepack pnpm --filter @frontier/web test`, `corepack pnpm smoke:e2e`, and `node scripts/web_game_playwright_client.js --url http://localhost:5173/login --click-selector \"[data-demo-login='demo_alpha']\" --actions-file output/web-game-worldmap/actions.json --iterations 1 --pause-ms 500 --screenshot-dir output/web-game-alliance-fx`; all passed.
- 2026-03-18: Added a deterministic smoke fixture user (`demo_smoke` / `Ironvein`) plus `apps/server/prisma/resetSmokeFixture.ts`. The browser smoke script now resets that fixture before every run, guaranteeing fresh troops, a nearby city, and alliance membership without disturbing the normal demo accounts.
- 2026-03-18: Updated `scripts/smoke_kingdom_core.mjs` to default to `demo_smoke` and launch the reset fixture through the local `tsx` CLI before opening the browser. Smoke validation now consistently exercises a real `BARBARIAN_ATTACK` path and produces `Ironvein -> Barbarian Camp 5` instead of falling back to a scout report.
- 2026-03-18: Added a new map command layer on `/app/map`. Players can now search the visible chunk for cities, POIs, and alliance markers, then focus the camera directly from the result list. The same panel can post alliance markers for either the current camera center or the currently selected target without leaving the map route.
- 2026-03-18: Live browser validation confirmed the new command layer end-to-end: searching `Ironvein` re-centered the map to `28,32`, and posting `Smoke flank` from the map route immediately added the marker to the minimap and the quick-focus marker row.
- 2026-03-18: Added field-command interactions to the world-space map. Right-clicking the Phaser world now opens a `Field Command` sheet for the nearest POI/city or raw tile, with quick actions for focus, target opening, scouting, and alliance-marker posting directly from the map scene.
- 2026-03-18: Exposed the field-command state to automation through `window.frontierMapFieldCommand` and `window.open_map_field_command(...)`, then validated the new sheet in a headless Playwright run by opening `Barbarian Camp 5`, posting `Field ping 1481`, and capturing `output/field-command-live.png`.
- 2026-03-18: Re-ran `corepack pnpm --filter @frontier/web build`, `corepack pnpm --filter @frontier/web test`, `node scripts/web_game_playwright_client.js --url http://localhost:5173/login --click-selector \"[data-demo-login='demo_alpha']\" --actions-file output/web-game-worldmap/actions.json --iterations 1 --pause-ms 500 --screenshot-dir output/web-game-field-command`, and `corepack pnpm smoke:e2e`; all passed after the field-command pass.
- 2026-03-19: Added a dedicated `corepack pnpm smoke:field-command` Playwright scenario in `scripts/smoke_map_field_command.mjs`. It resets the deterministic `demo_smoke` fixture, opens the map route, targets a visible frontier POI, opens the `Field Command` sheet, posts a fresh alliance marker, and captures `output/map-field-command-e2e.png`.
- 2026-03-19: The new field-command scenario records whether the interaction originated from a real canvas right-click or the `window.open_map_field_command(...)` automation hook. In the current headless environment it fell back to `automation-hook`, but it still validated the full sheet + marker-post loop and produced marker `Field ping 6747` at `30,30`.
- 2026-03-19: Re-ran `corepack pnpm smoke:field-command`, `node scripts/web_game_playwright_client.js --url http://localhost:5173/login --click-selector \"[data-demo-login='demo_alpha']\" --actions-file output/web-game-worldmap/actions.json --iterations 1 --pause-ms 500 --screenshot-dir output/web-game-field-command-pass`, and `corepack pnpm smoke:e2e`; all passed after the new dedicated scenario landed.
- 2026-03-19: Added a `Rapid Orders` layer to `/app/map` with keyboard shortcuts and a recent-marker operations list. The command strip now exposes searchable hotkeys, recent alliance marker focus buttons with age labels, and direct shortcuts for search, marker input, camera markers, recenter, zoom, marker cycling, and `1-4` filter changes.
- 2026-03-19: Hardened map keyboard handling to use `KeyboardEvent.code` instead of locale-sensitive `key` checks. `Esc` now blurs focused inputs before closing panels, which fixed the search-to-marker shortcut chain on international keyboard layouts.
- 2026-03-19: Re-ran `corepack pnpm --filter @frontier/web build`, `corepack pnpm --filter @frontier/web test`, `corepack pnpm smoke:e2e`, and a targeted Playwright hotkey check. Validation confirmed `/` focuses the search field, `Esc` releases it, and `M` correctly moves focus to the marker label input.
- 2026-03-19: Added alliance marker removal support across the stack. Alliance markers now expose ownership/delete capability in shared DTOs, the backend supports `DELETE /api/game/alliance/markers/:id`, and both `/app/map` rapid-order markers and `/app/alliance` marker cards can remove markers with officer-or-owner permissions.
- 2026-03-19: Validated marker removal with `corepack pnpm --filter @frontier/shared build`, `corepack pnpm --filter @frontier/server build`, `corepack pnpm --filter @frontier/web build`, `corepack pnpm --filter @frontier/web test`, `node scripts/web_game_playwright_client.js ...`, `corepack pnpm smoke:field-command`, and a targeted Playwright create-then-remove marker scenario that captured `output/alliance-marker-delete.png`.
- 2026-03-19: Added timed alliance markers. New markers now receive a 6-hour `expiresAt`, legacy markers are backfilled and pruned during `reconcileWorld()`, and expired markers broadcast `alliance.updated` so map/alliance UI stays in sync without manual refresh.
- 2026-03-19: Surfaced marker expiry in the UI. Map rapid-order rows and alliance marker cards now show remaining lifetime, and `smoke:field-command` asserts that freshly posted markers include an expiration timestamp.
- 2026-03-19: Tightened the map route into a viewport-locked shell. `GameLayout` now keeps scrolling inside the content panel instead of the page body, which stops the world-map HUD from drifting when the mouse wheel is used and makes the app behave like a fixed strategy command console.
- 2026-03-19: Reworked `/app/map` into a more map-first battlefield layout. The Phaser map now sits in a sticky left stage, command/search/marker tools moved into a scrollable right operations rail, and `WorldMap` now blocks browser wheel scroll while the pointer is over the canvas.
- 2026-03-19: Added another visual polish pass to the map experience: the map frame remains above the fold on desktop, the operations rail gets its own scrollbar, and the overall route feels steadier during wheel zoom and long-session navigation.
- 2026-03-19: Re-ran `corepack pnpm --filter @frontier/web build`, `corepack pnpm --filter @frontier/web test`, targeted Playwright map-wheel/layout checks, and `corepack pnpm smoke:e2e`; validation stayed green, with `window.scrollY` remaining `0` during wheel interaction and the smoke run still completing via `Ironvein -> Barbarian Camp 5`.
- Note: `corepack pnpm smoke:e2e` is still green when run sequentially, but it should not be launched in parallel with `smoke:field-command` because both reset the deterministic `demo_smoke` fixture and can race on username creation.
- TODO: Investigate why Phaser canvases are not consistently discoverable in headless Playwright on the map route, then upgrade `smoke:field-command` from its current automation-hook fallback to guaranteed canvas-originated right-click coverage.
- 2026-03-19: Extended Phase 2B battle windows beyond city-only staging. POI-targeted `BARBARIAN_ATTACK` and `RESOURCE_GATHER` marches now enter shared `BattleWindow` resolution on the server, with staged participants exposed in map DTOs for both cities and POIs.
- 2026-03-19: Added new shared UI/data layers for commander progression and alliance role management. Shared commander DTOs now expose a talent-tree structure, alliance state includes donation history, `AllianceRole` now includes `RECRUIT`, and the web app now lazy-loads `/app/commanders` and `/app/alliance/roles`.
- 2026-03-19: Implemented `CommanderPage` and `AllianceRolesPage`, plus reusable `CommanderSkillTree` and `BattleWindowPanel` components. The commander page shows roster XP/progression and talent layout, while the alliance roles page covers role reassignment, treasury donation input, and contribution/donation history.
- 2026-03-19: Upgraded `/app/map` with React-side battle-window context and new visibility toggles for `Show Paths`, `Show Scout Trails`, and `Show Reports`. `WorldMap` now supports report balloons, report click-through to `/app/reports?focus=...`, and optional hiding of march paths/scout trails.
- 2026-03-19: Added client-side report filtering on `/app/reports` for category, outcome, and date window, plus focus highlighting for map-driven report deep links.
- 2026-03-19: Added a theme provider and day/night/high-contrast mode switch in `GameLayout`, backed by shared design tokens in `packages/shared/src/design/tokens.ts` and CSS variable overrides in `apps/web/src/styles/tokens.css`.
- 2026-03-19: Wrote manual migration `20260319163000_phase2c_alliance_roles_poi_windows` for `AllianceDonation`, `AllianceRole.RECRUIT`, and POI-aware battle-window columns. Local validation DB was brought up to date with `corepack pnpm --filter @frontier/server prisma:push` because the existing dev database was not baselined for `migrate deploy`.
- 2026-03-19: Added Vitest render checks for `BattleWindowPanel` and `CommanderSkillTree`, then re-ran `corepack pnpm --filter @frontier/shared build`, `corepack pnpm --filter @frontier/server build`, `corepack pnpm --filter @frontier/web build`, `corepack pnpm --filter @frontier/web test`, `corepack pnpm smoke:e2e`, and `node scripts/web_game_playwright_client.js ...`; all passed.
- 2026-03-19: Live browser validation confirmed the new `/app/commanders` and `/app/alliance/roles` routes render under the active HUD shell with the refreshed English copy and current demo data.
- 2026-03-19: Updated the server smoke suite to match the new commander collection and POI battle-window mechanics. API smoke now asserts `STAGING -> resolve` for barbarian camps and resource nodes, exercises the standalone `/api/alliance` donation/role routes, and checks that `/api/game/commanders` returns populated skill-tree payloads.
- 2026-03-19: Re-ran `corepack pnpm --filter @frontier/server test` and `corepack pnpm --filter @frontier/web test`; both suites are green again after the regression alignment pass.
- 2026-03-29: Reworked the `/app/map` target/action sheet into a heavier command-tray pass. `MapPage` now opens target-side action cards for scout/rally/attack-or-gather, adds preview cards for ETA/power/carry and battle windows, and gives the composer flow a clearer four-step command strip with commander/loadout summary cards.
- 2026-03-29: Widened the desktop aside `BottomSheet` to better fit the new command tray and kept web validation green with `corepack pnpm --filter @frontier/web build` plus `corepack pnpm --filter @frontier/web test`.
- 2026-03-29: Attempted `corepack pnpm smoke:field-command`, but it is currently blocked because the local dev server is not running on `http://localhost:5173` (`ERR_CONNECTION_REFUSED`).
- TODO: Add or extend a smoke scenario that explicitly opens the new target command tray from map selection so this richer target-side action flow is covered by browser automation, not just the existing field-command scenario.

- 2026-03-29: Added a new `Camera UX` rail to `/app/map`. The map stage now surfaces drag/zoom guidance, chunk-pull metrics, focus state, and a three-step detail ladder so far/mid/near zoom changes feel intentional instead of hidden.
- 2026-03-29: Exposed map overlay state through `window.frontierMapUi` and `render_game_to_text`, then extended `smoke:field-command` to validate the richer `Field Command -> Open Target -> Command Tray` flow before reopening the field-command sheet and posting an alliance marker.
- 2026-03-29: Made Vite's dev proxy target configurable through `FRONTIER_API_PROXY_TARGET`, `FRONTIER_WS_PROXY_TARGET`, and `FRONTIER_WEB_PORT`, and switched the web socket client to same-origin `/ws`. This avoids unrelated local port collisions and keeps browser smoke aligned with whichever repo server is active.
- 2026-03-29: Re-ran `corepack pnpm --filter @frontier/web build`, `corepack pnpm --filter @frontier/web test`, and `corepack pnpm smoke:field-command`. The smoke pass is green again on the repo-owned stack and now confirms both target-tray validation plus marker creation (`Barbarian Camp 5`, `Field ping 2896`).

- 2026-03-30: Reworked the /app/map side rail into a stronger operations board. Aegis Command now opens with live field-status cards for orders, battle windows, signal rail, and visible intel so map sessions keep strategic context without opening a drawer.
- 2026-03-30: Upgraded the MILITARY EVENTS list with objective chips, live progress rails, troop/cargo footnotes, and selected-card emphasis. Active marches now read like premium field orders instead of generic list rows.
- 2026-03-30: Rebuilt Visible history into richer intel cards with clearer tone badges and coordinate labels, keeping report beacons closer to the same visual language as the new command trays.
- 2026-03-30: Re-ran corepack pnpm --filter @frontier/web build, corepack pnpm --filter @frontier/web test, and corepack pnpm smoke:field-command against the repo-owned dev stack (PORT=3102, FRONTIER_API_PROXY_TARGET=http://localhost:3102). All passed, and the smoke artifact refreshed output/map-field-command-e2e.png with marker Field ping 8313.
- 2026-03-30: Reworked `/app/reports` into a stronger council bridge. War Council now surfaces dispatch handoff counts, richer dossier metadata, quick redeploy/archive actions, and an adjacent chronicle so report review feels connected to both `/app/map` and `/app/messages`.
- 2026-03-30: Upgraded `/app/messages` with a dispatch board layer. Message Center now summarizes archive groups, shows route recommendations into map/reports/market, and gives each selected dispatch a clearer operations bridge plus reward/archive posture.
- 2026-03-30: Re-ran `corepack pnpm --filter @frontier/web build` and `corepack pnpm --filter @frontier/web test` after the War Council and Message Center pass; both stayed green. Browser smoke was not rerun in this pass because the changes were route-level dossier/archive UI rather than map-canvas behavior.
- 2026-03-30 22:35 Closed alpha release-prep pass completed: added `/api/public/bootstrap`, `REGISTRATION_MODE` + `STORE_ENABLED` gating, production env hardening, alpha user provisioning CLI, web bootstrap-driven auth/store behavior, release badge/version stamping, and `deploy/` runbook templates for Caddy/systemd/Postgres backup. Validation: `corepack pnpm build`, `corepack pnpm test`, `corepack pnpm --filter @frontier/server build`, `corepack pnpm --filter @frontier/web build`. Smoke not run in this pass because no local app instance was listening on `localhost:5173` / `localhost:3101`.
- 2026-03-30 23:58 Closed-alpha UX hardening pass: hid demo-login banners on invite-only auth, replaced them with operator-provisioned access copy, added `smoke:alpha-access` plus a repo-owned `smoke:alpha` suite that boots a local closed-alpha stack (`PORT=3103`, `FRONTIER_WEB_PORT=4173`) and validates access gating, kingdom core, and field-command flows end-to-end.
- 2026-03-30 23:58 Rewrote `scripts/smoke_kingdom_core.mjs` around the current command-tray labels (`Attack Camp`, `Gather Here`, `Attack City`) so browser automation tracks the premium map flow instead of the old `Proceed` step. Validation: `corepack pnpm --filter @frontier/web build`, `corepack pnpm --filter @frontier/web test`, and `corepack pnpm smoke:alpha` all passed, refreshing `output/closed-alpha-access-e2e.png`, `output/kingdom-core-e2e.png`, and `output/map-field-command-e2e.png`.
- 2026-04-01: Added frontend regression coverage for closed-alpha release guards. New web tests now lock the login-only auth surface, `/app/market` redirect behavior, and hidden store quick action; `GameLayout` also force-closes the market preview if launch flags disable commerce mid-session.
- 2026-04-01: Validation pass: `corepack pnpm --filter @frontier/web test` and `corepack pnpm --filter @frontier/web build` passed after syncing workspace installs with `corepack pnpm install`. A follow-up `corepack pnpm smoke:alpha` attempt was blocked by local environment state because Docker Desktop/Postgres was not running (`//./pipe/dockerDesktopLinuxEngine` missing), not by a web/runtime regression.
- 2026-04-01: Hardened the smoke toolchain with a shared `scripts/smoke_support.mjs` helper. `smoke:alpha` now fails early for port collisions, unreachable custom `DATABASE_URL` targets, and missing Docker/Postgres prerequisites instead of dying with opaque spawn errors; the suite also dumps the last server/web log lines when startup fails mid-run.
- 2026-04-01: Upgraded browser selectors for release guards and map automation. `GameLayout` now emits stable `data-nav-item`, `data-archive-item`, `data-quick-action`, `data-release-badge`, and `data-version-stamp` markers; `WorldMap` exposes a stable `data-map-canvas` root and `MapPage` exposes `window.focus_map_target(...)` so `smoke:field-command` can center a real canvas interaction before falling back to the field-command hook.
- 2026-04-01: Added route-level shell regression coverage in `GameLayout.test.tsx`. Web tests now lock closed-alpha archive visibility plus the positive case where market navigation returns under public/store-enabled bootstrap flags.
- 2026-04-01: Tightened map runtime cost without stripping the premium look. `WorldMap` now throttles route-line redraws through a snapshot cache, uses more aggressive far/mid/near detail thresholds, and reduces trail-dust frequency/scale at wider zoom levels.
- 2026-04-01: Consolidated repeated premium surfaces into token-driven gradients and shadows. `tokens.css` now owns shared hero/card/active/running surface variables, and `Reports`, `Message Center`, `Leaderboard`, and `Research` all consume those tokens instead of repeating page-local hardcoded gradients.
- 2026-04-01: Split alpha vs public-launch scope more explicitly by adding `deploy/public-launch-backlog.md` and linking it from the deploy runbook. Deferred items now live in a dedicated doc instead of drifting into closed-alpha implementation work.
- 2026-04-01: Validation after the quality-hardening pass: `corepack pnpm --filter @frontier/web test` and `corepack pnpm --filter @frontier/web build` both passed. `corepack pnpm smoke:alpha` now fails with the intended prerequisite message on machines without Docker/Postgres: `Postgres is not reachable on localhost:5433 and Docker is not available from this shell...`.
